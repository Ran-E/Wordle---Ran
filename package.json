{
  "name": "wordle---ran",
  "version": "1.0.0",
  "description": "Word guessing game",
  "main": "index.js",
  "scripts": {
    "build": "sass src/styles/style.scss src/styles/style.css",
    "dev-server": "live-server src",
    "dev-sass": "sass src/styles/style.scss src/styles/style.css --watch",
    "dev": "npm run dev-server & npm run dev-sass",
    "start": "echo \"No start script\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ran Elbaz",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.56.1"
  }
}
